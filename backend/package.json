{
  "type": "module",
  "scripts": {
    "start": "nodemon dist/api.js",
    "start:debug": "nodemon --inspect-brk dist/api.js",
    "clean": "cross-env rm -rf ./dist",
    "check-circular-deps": "madge --circular dist/api.js",
    "build:watch": "npm run copy-files && tsc -w",
    "build": "npm run clean && tsc"
  },
  "dependencies": {
    "@google-cloud/common": "^5.0.0",
    "@google-cloud/storage": "^7.2.0",
    "cross-env": "^7.0.3",
    "express": "^4.18.2",
    "express-formidable": "1.2.0",
    "nodemon": "^3.0.1",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.18",
    "@types/express-formidable": "^1.2.1"
  }
}
